export interface AppointmentsConfig {
  appointmentPrompt: {
    buttonLabel: string;
    heading: string;
  };
  supportBox: {
    heading: string;
    description: string;
    buttonLabel: string;
  };
}

